import { Contract } from 'ethers';
import { DeFiSDKInterface } from './protocols/interfaces';
import { Address } from './protocols/types';
export declare class DeFiSDK implements DeFiSDKInterface {
    protected adapterRegistry: Contract;
    private builder;
    constructor(nodeUrl: string);
    getProtocolNames(): Promise<string[]>;
    getProtocolMetaData(protocol: string): any;
    getTokenAdapterNames(): Promise<string[]>;
    getProtocolBalance(account: Address, protocol: string): any;
    getProtocolBalances(account: Address, protocols: string[]): any;
    getAccountBalances(account: Address): any;
    getTokenComponents(type: string, token: Address): any;
}
