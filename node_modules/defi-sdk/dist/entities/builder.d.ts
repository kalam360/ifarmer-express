import { AdapterBalance, AdapterMetadata, AssetBalance, ProtocolBalance, ProtocolMetadata, TokenBalance, TokenMetadata } from './entities';
export declare class Builder {
    protocolBalance(protocolBalance: any): ProtocolBalance;
    adapterBalance(protocolAdapter: any): AdapterBalance;
    protocolMetadata(protocol: any): ProtocolMetadata;
    adapterMetadata(adapterMetadata: any): AdapterMetadata;
    assetBalance(assetBalance: any): AssetBalance;
    tokenBalance(tokenBalance: any): TokenBalance;
    tokenMetadata(metadata: any): TokenMetadata;
}
